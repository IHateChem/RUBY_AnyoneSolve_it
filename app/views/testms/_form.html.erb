<%= form_with(model: testm) do |form| %>
  <% if testm.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(testm.errors.count, "error") %> prohibited this testm from being saved:</h2>

      <ul>
        <% testm.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ids, style: "display: block" %>
    <%= form.text_field :ids %>
  </div>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.text_field :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
